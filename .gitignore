.venv/
.env
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Generated benchmark output (loadtest/run_benchmark.py) — not source.
loadtest/results/

# Generated chaos-suite output (loadtest/chaos/run_all.py) — raw per-poll
# timelines and k6 dumps, regenerable via `make chaos`; the curated
# analysis lives in docs/chaos-results.md, which IS committed.
loadtest/chaos/results/

# Per-process Prometheus metric files (multiprocess mode) — not source.
.prometheus-multiproc/
.prometheus-multiproc-chaos/

# Frontend
web/node_modules/
web/dist/

# Raw Playwright video recordings behind docs/demo.gif — large, easily
# regenerated by web/demo/two-browsers.mjs, not source.
web/demo/recordings/
